

|**Идентификатор**|**Приоритет**|**Требование**|**Название**|**Приготовления**|**Ход выполнения**|**Постусловия**|**Ожидаемый результат**|
| :-: | :-: | :-: | :-: | :-: | :-: | :-: | :-: |
|correct\_1|**A**|3|Получение данных о созданном сотруднике|<p>1. Создать объект с ключами name:string, email:string, position:string, age:integer в формате json и заполнить его значения произвольными, но корректными данными</p><p>2. Создать сотрудника методом Post</p><p>3. Запомнить его id</p>|<p>1. Ввести в параметр метода Get id с типом string ранее созданного сотрудника</p><p>2. Отобразить его данные с помощью метода Get </p>||<p>1. id отображается на экране в параметре Get</p><p>2. Отображается ранее созданный сотрудник с его данными</p>|
|correct\_2|**A**|4|Добавление нового сотрудника с корректными данными||<p>1. Создать объект с ключами и значениями: name:“Kirill”, email:“[test123@test.ru](mailto:test@test.ru)”, position:“CEO”, age:23 в формате json</p><p></p><p>2. Методом Post добавить нового сотрудника </p><p></p>|При вызове метода Get с id созданного сотрудника, отображаются данные о сотруднике|<p>1. Объект создан и отображается на экране</p><p>2. Отображается сообщение об успешном добавлении сотрудника и его id</p><p></p>|
|correct\_3|**A**|5|Удаление сотрудника по  id|<p>1. Создать сотрудника методом Post, введя произвольные, но корректные данные</p><p>2. Запомнить его id</p>|<p>1. Ввести id с типом integer ранее созданного сотрудника в параметр Delete</p><p>2. Удалить данного сотрудника с помощью метода Delete</p><p>3. Посмотреть на ответ от сервера( в формате json и иметь message)</p><p>4. Посмотреть на код ответа от сервера </p><p></p>|<p>1. Вызвать метод Get по id ранее сохраненному и получить ответ 404 ошибки</p><p></p>|<p>1. id отображается на экране в методе Delete</p><p>2. Сообщение об удалении сотрудника </p><p>3. Отображается сообщение об отсутствии такого сотрудника </p><p>4. Код ответа 204</p>|
|incrt\_2\_1|**B**|2|Получение сотрудника по id, которое имеет тип float|1. Создать переменную с типом данных float и дать ей произвольное float-значение |<p>1. Ввести id с 	типом float в параметр Get </p><p>1. Отобразить его данные с помощью метода Get </p><p></p><p></p>||<p>1. id отображается на экране</p><p>2. Отображается сообщение о некорректном id</p>|
|incrt\_2\_2|**A**|2|Получение сотрудника по id, которое имеет тип null|1. Создать переменную с типом данных null|<p>1. Ввести в параметр метода Get добавленную переменную </p><p>2. Отправить запрос Get</p><p></p><p></p>||<p>1. Параметр отображается на экране</p><p>2. Отображается сообщение о некорректном id</p>|
|incrt\_2\_3|**A**|2|Получение сотрудника по id, которое имеет тип integer, но отрицательное значение|<p>1. Создать переменную с типом данных integer</p><p>2. Присвоить ей значение -1</p>|<p>1. Ввести в параметр метода Get добавленную переменную </p><p>2. Отправить запрос Get</p>||<p>1. Параметр отображается на экране</p><p>2. Отображается сообщение о некорректном id</p>|
|incrt\_2\_5|**C**|2|Получение сотрудника по id типа boolean|<p>1. Создать переменную с типом данных boolean</p><p>2. Присвоить ей значение true</p>|<p>1. Ввести в параметр метода Get добавленную переменную </p><p>2. Отправить запрос Get</p>||<p>1. Параметр отображается на экране</p><p>2. Отображается сообщение о некорректном id</p>|
|incrt\_2\_6|**A**|2|Работа запроса Get при вводе ему пустой строки в параметр|<p>1. Создать переменную с типом данных string</p><p>2. Присвоить ей **значение =”” (пустая строка)**</p>|<p>1. Ввести в параметр метода Get добавленную переменную </p><p>2. Отправить запрос Get</p>||<p>1. Параметр отображается на экране</p><p>2. Отображается сообщение о некорректном id</p>|
|incrt\_4\_1|**A**|4\.1|Post запрос объекта с name:null |<p>1. Создать объект с ключами **name:null,** email:string, position:string, age:integer в формате json</p><p>2. Заполнить все значения, кроме name,произвольными, но корректными данными согласно документации</p>|<p>1. Вставить объект в параметр метода Post</p><p>2. Методом Post добавить нового сотрудника </p><p></p><p></p><p></p>||<p>1. Параметр отображается на экране</p><p>2. Отображается сообщение об ошибке добавления 400</p>|
|incrt\_4\_1\_2|**A**|4\.1|Post запрос объекта с name:integer |<p>1. Создать объект с ключами name:integer, email:string, position:string, age:integer в формате json</p><p>2. Заполнить **name=1234,** остальные ключи заполнить произвольными, но корректными данными согласно документации</p>|<p>1. Вставить объект в параметр метода Post</p><p>2. Методом Post добавить нового сотрудника </p>||<p>1. Параметр отображается на экране</p><p>2. Отображается сообщение об ошибке добавления 400</p>|
|incrt\_4\_1\_3|**A**|4\.1|Post запрос объекта с name:**“”**|<p>1. Создать объект с ключами name:string, email:string, position:string, age:integer в формате json</p><p>2. **Заполнить name=“”**, остальные ключи заполнить произвольными, но корректными данными согласно документации</p>|<p>1. Вставить объект в параметр метода Post</p><p>2. Методом Post добавить нового сотрудника </p>||<p>1. Параметр отображается на экране</p><p>2. Отображается сообщение об ошибке добавления 400</p>|
|incrt\_4\_1\_4|**C**|4\.1|Post запрос объекта с name:float|<p>1. Создать объект с ключами name:float, email:string, position:string, age:integer в формате json</p><p>2. Заполнить **name=1.0,** остальные ключи заполнить произвольными, но корректными данными согласно документации</p>|<p>1. Вставить объект в  параметр метода Post </p><p>2. Методом Post добавить нового сотрудника</p><p></p>||<p>1. Параметр отображается на экране</p><p>2. Отображается сообщение об ошибке добавления 400</p>|
|incrt\_4\_2\_1|**A**|4\.2|Ответ на Post запрос объекта с email типа integer|<p>1. Создать объект с ключами name:string, email:integer, position:string, age:integer в формате json</p><p>2. Заполнить **email=123,** остальные ключи заполнить произвольными, но корректными данными согласно документации</p>|<p>1. Вставить объект в  параметр метода Post </p><p>2. Методом Post добавить нового сотрудника</p>||<p>1. Параметр отображается на экране</p><p>2. Отображается сообщение об ошибке добавления 400</p>|
|incrt\_4\_2\_2|**A**|4\.2|Ответ на Post запрос объекта без email (null)|<p>1. Создать объект с ключами name:string, **email:null,** position:string, age:integer в формате json</p><p>2. Заполнить все ключи произвольными, но корректными данными согласно документации</p>|<p>1. Вставить объект в  параметр метода Post </p><p>2. Методом Post добавить нового сотрудника</p>||<p>1. Параметр отображается на экране</p><p>2. Отображается сообщение об ошибке добавления 400</p>|
|incrt\_4\_2\_3|**A**|4\.2|Ответ на Post запрос объекта с email =””|<p>1. Создать объект с ключами name:string, email:string, position:string, age:integer в формате json</p><p>2. **Заполнить email=””** (пустая строка), все остальные ключи произвольными, но корректными данными согласно документации</p>|<p>1. Вставить объект в  параметр метода Post </p><p>2. Методом Post добавить нового сотрудника</p>||<p>1. Параметр отображается на экране</p><p>2. Отображается сообщение об ошибке добавления 400</p>|
|incrt\_4\_2\_4|**A**|4\.2|Ответ на Post запрос объекта с неуникальным email|<p>1. Создать объект с ключами name:string, email:string, position:string, age:integer в формате json и заполнить его произвольными и корректными значениями, введя в 'email':'<test123@test.ru>'.</p><p></p><p></p>|<p>1. Вставить объект в  параметр метода Post</p><p>2. Методом Post добавить нового сотрудника</p><p>3. Повторно отправить объект из пункта 1 запросом Post</p>||<p>1. Параметр отображается на экране</p><p>2. Отображается сообщение об успешности добавления 201</p><p>3. Отображается сообщение об ошибке добавления 400</p>|
|incrt\_4\_2\_5|**A**|4\.2|Ответ на Post запрос объекта с email без имени пользователя|<p>1. Создать объект с ключами name:string, email:string, position:string, age:integer в формате json</p><p>2. Заполнить **email=”@test.ru” ,** все остальные ключи произвольными, но корректными данными согласно документации</p>|<p>1. Вставить объект в  параметр метода Post</p><p>2. Методом Post добавить нового сотрудника</p>||<p>1. Параметр отображается на экране</p><p>2. Отображается сообщение об ошибке добавления 400</p>|
|incrt\_4\_2\_6|**A**|4\.2|Ответ на Post запрос объекта с email без имени сервера|<p>1. Создать объект с ключами name:string, email:string, position:string, age:integer в формате json</p><p>2. **Заполнить email=”test123@.ru” ,** все остальные ключи произвольными, но корректными данными согласно документации</p>|<p>1. Вставить объект в  параметр метода Post</p><p>2. Методом Post добавить нового сотрудника</p>||<p>1. Параметр отображается на экране</p><p>2. Отображается сообщение об ошибке добавления 400</p>|
|incrt\_4\_2\_7|**A**|4\.2|Ответ на Post запрос объекта с email без домена|<p>1. Создать объект с ключами name:string, email:string, position:string, age:integer в формате json</p><p>2. **Заполнить email=**</p><p>**”test123@test.”** , все остальные ключи произвольными, но корректными данными согласно документации</p>|<p>1. Вставить объект в  параметр метода Post</p><p>2. Методом Post добавить нового сотрудника</p>||<p>1. Параметр отображается на экране</p><p>2. Отображается сообщение об ошибке добавления 400</p>|
|incrt\_4\_2\_8|**A**|4\.2|Ответ на Post запрос объекта с email без @|<p>1. Создать объект с ключами name:string, email:string, position:string, age:integer в формате json</p><p>2. **Заполнить email=**</p><p>**”test123test.ru”** , все остальные ключи произвольными, но корректными данными согласно документации</p>|<p>1. Вставить объект в  параметр метода Post</p><p>2. Методом Post добавить нового сотрудника</p>||<p>3. Параметр отображается на экране</p><p>4. Отображается сообщение об ошибке добавления 400</p>|
|incrt\_4\_2\_9|**A**|4\.2|Ответ на Post запрос объекта с email без точки перед доменом|<p>1. Создать объект с ключами name:string, email:string, position:string, age:integer в формате json</p><p>2. **Заполнить email=**</p><p>**”test123@testru”** , все остальные ключи произвольными, но корректными данными согласно документации</p>|<p>1. Вставить объект в  параметр метода Post</p><p>2. Методом Post добавить нового сотрудника</p>||<p>1. Параметр отображается на экране</p><p>2. Отображается сообщение об ошибке добавления 400</p>|
|incrt\_4\_2\_10|**A**|4\.2|Ответ на Post запрос объекта с email без точки перед доменом и @|<p>1. Создать объект с ключами name:string, email:string, position:string, age:integer в формате json</p><p>2. **Заполнить email=**</p><p>**”test123testru”** , все остальные ключи произвольными, но корректными данными согласно документации</p>|<p>1. Вставить объект в  параметр метода Post</p><p>2. Методом Post добавить нового сотрудника</p>||<p>1. Параметр отображается на экране</p><p>2. Отображается сообщение об ошибке добавления 400</p>|
|incrt\_4\_3\_1|**A**|4\.3|Ответ на Post запрос объекта с position:boolean|<p>1. Создать объект с ключами name:string, email:string, **position:boolean**, age:integer в формате json</p><p>2. Заполнить position=</p><p>true, все остальные ключи произвольными, но корректными данными согласно документации</p>|<p>1. Вставить объект в  параметр метода Post</p><p>2. Методом Post добавить нового сотрудника</p>||<p>1. Параметр отображается на экране</p><p>2. Отображается сообщение об ошибке добавления 400</p>|
|incrt\_4\_3\_2|**A**|4\.3|Ответ на Post запрос объекта с position:null|<p>1. Создать объект с ключами name:string, email:string, **position:null**, age:integer в формате json</p><p>2. Заполнить все ключи произвольными, но корректными данными согласно документации</p>|<p>1. Вставить объект в  параметр метода Post</p><p>2. Методом Post добавить нового сотрудника</p>||<p>1. Параметр отображается на экране</p><p>2. Отображается сообщение об ошибке добавления 400</p>|
|incrt\_4\_3\_3|**A**|4\.3|Ответ на Post запрос объекта с position:integer|<p>1. Создать объект с ключами name:string, email:string, **position:integer**, age:integer в формате json</p><p>2. Заполнить position=1, все остальные ключи заполнить произвольными, но корректными данными согласно документации</p>|<p>1. Вставить объект в  параметр метода Post</p><p>2. Методом Post добавить нового сотрудника</p>||<p>1. Параметр отображается на экране</p><p>2. Отображается сообщение об ошибке добавления 400</p>|
|incrt\_4\_3\_4|**A**|4\.3|Ответ на Post запрос объекта с position:””|<p>1. Создать объект с ключами name:string, email:string, position:string, age:integer в формате json</p><p>2. Заполнить position=””, все остальные ключи заполнить произвольными, но корректными данными согласно документации</p>|<p>1. Вставить объект в  параметр метода Post</p><p>2. Методом Post добавить нового сотрудника</p>||<p>1. Параметр отображается на экране</p><p>2. Отображается сообщение об ошибке добавления 400</p>|
|incrt\_4\_4\_1|**A**|4\.4|Ответ на Post запрос объекта с age:null|<p>1. Создать объект с ключами name:string, email:string, position:string, age:null в формате json</p><p>2. Все ключи, кроме age, заполнить произвольными, но корректными данными согласно документации</p>|<p>1. Вставить объект в  параметр метода Post</p><p>2. Методом Post добавить нового сотрудника</p>||<p>1. Параметр отображается на экране</p><p>2. Отображается сообщение об ошибке добавления, ошибка 400</p>|
|incrt\_4\_4\_2|**A**|4\.4|Ответ на Post запрос объекта с age:string, но с числом (“32”)|<p>1. Создать объект с ключами name:string, email:string, position:string, age:string в формате json</p><p>2. Заполнить age=”32”, все остальные ключи заполнить произвольными, но корректными данными согласно документации</p>|<p>1. Вставить объект в  параметр метода Post</p><p>2. Методом Post добавить нового сотрудника</p>||<p>1. Параметр отображается на экране</p><p>2. Отображается сообщение об ошибке добавления, ошибка 400</p>|
|incrt\_4\_4\_3|**A**|4\.4|Ответ на Post запрос объекта с age:””|<p>1. Создать объект с ключами name:string, email:string, position:string, age:string в формате json</p><p>2. Заполнить age=””, все остальные ключи заполнить произвольными, но корректными данными согласно документации</p>|<p>1. Вставить объект в  параметр метода Post</p><p>2. Методом Post добавить нового сотрудника</p>||<p>1. Параметр отображается на экране</p><p>2. Отображается сообщение об ошибке добавления, ошибка 400</p>|
|incrt\_4\_4\_4|**C**|4\.4|Отправка объекта с ключом age:boolean|<p>1. Создать объект с ключами name:string, email:string, position:string, age:boolean в формате json</p><p>2. Заполнить age=true, все остальные ключи заполнить произвольными, но корректными данными согласно документации</p>|<p>1. Вставить объект в  параметр метода Post</p><p>2. Методом Post добавить нового сотрудника</p>||<p>1. Параметр отображается на экране</p><p>2. Отображается сообщение об ошибке добавления, ошибка 400</p>|
|incrt\_4\_4\_5|**A**|4\.4||<p>1. Создать объект с ключами name:string, email:string, position:string, age:string в формате json</p><p>2. Заполнить age=-1, все остальные ключи заполнить произвольными, но корректными данными согласно документации</p>|<p>1. Вставить объект в  параметр метода Post</p><p>2. Методом Post добавить нового сотрудника</p>||<p>1. Параметр отображается на экране</p><p>2. Отображается сообщение об ошибке добавления.Некорректный возраст</p>|
|incrt\_4\_0\_1|**A**|4\.4|Отправка объекта без поля name|<p>1. Создать объект с ключами email:string, position:string, age:integer в формате json</p><p>2. Все ключи заполнить произвольными, но корректными данными согласно документации</p>|<p>1. Вставить объект в  параметр метода Post</p><p>2. Методом Post добавить нового сотрудника</p>||<p>1. Параметр отображается на экране</p><p>2. Отображается сообщение об ошибке добавления, ошибка 400</p>|
|incrt\_4\_0\_2|**A**|4\.4|Отправка объекта без поля  email|<p>1. Создать объект с ключами<br>   name:string, position:string, age:integer в формате json</p><p>2. Все ключи заполнить произвольными, но корректными данными согласно документации</p>|<p>1. Вставить объект в  параметр метода Post</p><p>2. Методом Post добавить нового сотрудника</p>||<p>1. Параметр отображается на экране</p><p>2. Отображается сообщение об ошибке добавления, ошибка 400</p>|
|incrt\_4\_0\_3|**A**|4\.4|Отправка объекта без поля  position|<p>1. Создать объект с ключами<br>   name:string, email:string, age:integer в формате json</p><p>2. Все ключи заполнить произвольными, но корректными данными согласно документации</p>|<p>1. Вставить объект в  параметр метода Post</p><p>2. Методом Post добавить нового сотрудника</p>||<p>1. Параметр отображается на экране</p><p>2. Отображается сообщение об ошибке добавления, ошибка 400</p>|
|incrt\_4\_0\_4|**A**|4\.4|Отправка объекта без поля  age|<p>1. Создать объект с ключами<br>   name:string, email:string, position:string в формате json</p><p>2. Все ключи заполнить произвольными, но корректными данными согласно документации</p>|<p>1. Вставить объект в  параметр метода Post</p><p>2. Методом Post добавить нового сотрудника</p>||<p>1. Параметр отображается на экране</p><p>2. Отображается сообщение об ошибке добавления, ошибка 400</p>|
|incrt\_4\_0\_5|**A**|4\.4|Отправка объекта без полей |<p>1. Создать пустой ([null]) объект без ключей<br>   в формате json</p><p></p>|<p>1. Вставить объект в  параметр метода Post</p><p>2. Методом Post добавить нового сотрудника</p>||<p>1. Параметр отображается на экране</p><p>2. Отображается сообщение об ошибке добавления, ошибка 400</p>|
|incrt\_4\_0\_6|**A**|4\.4|Отправка объекта с лишним полем|<p>1. Создать объект с ключами<br>   name:string, email:string, position:string,</p><p>age:integer<br>isSingle:boolean в формате json</p><p>1. Все ключи заполнить произвольными, но корректными данными согласно документации, а ключ isSingle заполнить true</p>|<p>1. Вставить объект в  параметр метода Post</p><p>2. Методом Post добавить нового сотрудника</p>||<p>1. Параметр отображается на экране</p><p>2. Отображается сообщение об ошибке добавления, ошибка 400</p>|
|incrt\_4\_0\_7|**A**|4\.4|Отправка объекта с определенным id|<p>1. Создать объект с ключами</p><p>id:integer</p><p>name:string, email:string, position:string,</p><p>age:integer</p><p>в формате json</p><p>2. Все ключи заполнить произвольными, но корректными данными согласно документации, а ключ id заполнить 11111</p><p></p><p></p>|<p>1. Вставить объект в  параметр метода Post</p><p>2. Методом Post добавить нового сотрудника</p><p></p><p></p>||<p>1. Параметр отображается на экране</p><p>2. Отображается сообщение об ошибке добавления, ошибка 400</p><p></p>|
|incrt\_5\_1|**B**|2|Получение ответа на Delete с id = float|1. Создать переменную с типом данных float и дать ей произвольное float-значение |<p>1. Ввести id с 	типом float в параметр метода Delete</p><p>2. Удалить сотрудника с помощью метода Delete</p>||<p>1. id отображается на экране</p><p>2. Отображается сообщение о некорректном id и ошибка 404</p>|
|incrt\_5\_2|**A**|2|Получение ответа на Delete с id = null|1. Создать переменную с типом данных null|<p>1. Ввести в параметр метода Delete добавленную переменную с типом данных null </p><p>2. Удалить сотрудника с помощью метода Delete</p>||<p>1. Параметр отображается на экране</p><p>2. Отображается сообщение о некорректном id</p>|
|incrt\_5\_3|**A**|2|Получение ответа на Delete с id = -1|<p>1. Создать переменную с типом данных integer</p><p>2. Присвоить ей значение -1</p>|<p>1. Ввести в параметр метода Delete добавленную переменную </p><p>2. Отправить запрос Delete</p>||<p>1. Параметр отображается на экране</p><p>2. Отображается сообщение и ошибка 404</p>|
|incrt\_5\_4|**C**|2|Получение ответа на Delete с id = boolean|<p>1. Создать переменную с типом данных boolean</p><p>2. Присвоить ей значение true</p>|<p>1. Ввести в параметр метода Delete добавленную переменную </p><p>2. Отправить запрос Delete</p>||<p>1. Параметр отображается на экране</p><p>2. Отображается сообщение и ошибка 404</p>|
|incrt\_5\_5|**A**|2|Получение ответа на Delete с id = “”l|<p>1. Создать переменную с типом данных string</p><p>2. Присвоить ей **значение =”” (пустая строка)**</p>|<p>1. Ввести в параметр метода Delete добавленную переменную </p><p>2. Отправить запрос Delete</p>||<p>1. Параметр отображается на экране</p><p>2. Отображается сообщение и ошибка 404</p>|

