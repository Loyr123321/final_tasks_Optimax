

|**id**|**Тест-кейс**|**Название**|**Ожидаемый результат**|**Фактический результат**|**Шаги по воспроизведению**|**Воспроизводимость**|**Важность**|**Симптом**|**Возможность обойти**|**Комментарий**|
| :-: | :-: | :-: | :-: | :-: | :-: | :-: | :-: | :-: | :-: | :-: |
|1|correct\_3|После отправки запроса Delete с корректным id с типом string отсутствует сообщение  об успешном удалении (message) сотрудника|Отображается сообщение (message) с типом string после вызова Delete|Пустой объект json (json empty, без сообщения)|<p>1. Создать нового пользователя с корректными значениями и полями запросом Post</p><p>2. Передать в виде строки id пользователя из пункта 1 в url запроса Delete</p><p>3. Отправить запрос Delete	</p>|Always|Medium|Missing feature|Нет||
|2|correct\_3|При вызове запроса Delete с id существующего сотрудника приходит ответ 204 (удален), но он не удаляется из БД. |Вызов запроса Get с id удаленного сотрудника возвращает ответ 404.|При запросе Get с id удаленного сотрудника отображаются все его данные|<p>1. Добавить сотрудника с корректными данными запросом Post</p><p>2. Удалить созданного сотрудника по id, которое преобразовано в строку.</p><p>3. Получить данные об сотруднике запросом Get, указав его id</p>|Always|Critical|Missing feature|Нет||
|3|incrt\_4\_1\_3|При вызове Post-запроса сохраняется новый работник с именем = “”|Отображается ошибка валидации 400 ключа name|Отображается код ошибки 201 (создан) вместо 400-ой |<p>1. Создать объект name:””,<br>   email:test3@test.ru,<br>   position:”CEO”,</p><p>age:23  в формате json</p><p>2. Отправить запрос Post, вставив в параметр запроса объект</p>|Always|Critical|Missing feature|Нет||
|4|incrt\_4\_2\_4|При запросе Post создается работник с неуникальной почтой. |При создании сотрудника запросом Post отображается ошибка валидации 400|При создании сотрудника с неуникальной почтой запросом Post отображается ответ об успешности создания с кодом 201|<p>1. Создать объект с ключами             name : 'Kirill',           email: '<test1234455@test.ru>',<br>   position:'CEO',            age:23  в формате json</p><p>2. Отправить запрос Post с объектом из пункта 1</p><p>3. Создать объект с ключами             name : 'Mihail',           email: '<test1234455@test.ru>',<br>   position:'developer',            age:45  в формате json</p><p>4. Отправить запрос Post с объектом из пункта 2</p>|Always|Critical|Missing feature|Нет||
|5|incrt\_4\_3\_2|Отсутствие проверки на значение поля position (создался с null) Post запросом|При создании сотрудника Post запросом со значение position=null получает ответ ошибку 400|При создании сотрудника Post запросом со значение position=null получает ответ 201 (создался)|<p>1. Создать объект с ключами name:string, email:string, position:null, age:integer в формате json</p><p>2. Заполнить объект:<br>   'name':'Max7', email: 'test6@test.ru',<br>   position:null,            age:45                        'email':'test123@test.ru',              'position' null                  'age': 45</p><p>3. Вставить объект в  параметр метода Post</p><p>4. Методом Post добавить нового сотрудника</p>|Always|Critical|Missing feature|||
|6|incrt\_4\_3\_4|Отсутствие проверки на значение поля  position:“”|При создании сотрудника Post запросом со значение position=”” получает ответ ошибку 400|При создании сотрудника Post запросом со значение position=”” получает ответ 201 (создался)|<p>1. Создать объект с ключами name:string, email:string, position:null, age:integer в формате json</p><p>2. Заполнить объект:<br>   'name':'Max9', email: 'test8@test.ru',<br>   position:””,            age:45                        </p><p>3. Вставить объект в  параметр метода Post</p><p>4. Методом Post добавить нового сотрудника</p>|Always|Critical|Missing feature|Нет||
|7|incrt\_4\_4\_5|Создается запросом Post новый сотрудник с отрицательным возрастом|После отправки Post запроса приходит ответ ошибка с кодом 400|Создался новый сотрудник с отрицательным возрастом, код ответа 201|<p>1. Создать объект с ключами name:string, email:string, position:string, age:integer в формате json</p><p>2. Заполнить объект:<br>   'name':'Max14', email: ‘test13@test.ru',<br>   position:’developer23’, age:-1                        </p><p>3. Вставить объект в  параметр метода Post</p><p>4. Методом Post добавить нового сотрудника</p>|Always|Critical|Missing feature|||
|8|incrt\_4\_4\_4|Создается запросом Post новый сотрудник с возрастом true|После отправки Post запроса приходит ответ ошибка с кодом 400|Создался новый сотрудник с логическим возрастом, код ответа 201|<p>1. Создать объект с ключами name:string, email:string, position:string, age:boolean в формате json</p><p>2. Заполнить объект:<br>   'name':'Max13', email: ‘test12@test.ru',<br>   position:’developer23’, age:true                       </p><p>3. Вставить объект в  параметр метода Post</p><p>4. Методом Post добавить нового сотрудника</p>|Always|Critical|Missing feature|Нет||
|9|incrt\_4\_4\_3|Создается запросом Post новый сотрудник с возрастом в виде пустой строки|После отправки Post запроса приходит ответ ошибка с кодом 400|После отправки Post запроса приходит ответ ошибка с кодом 201|<p>1. Создать объект с ключами name:string, email:string, position:string, age:string в формате json</p><p>2. Заполнить объект:<br>   'name':'Max12', email: ‘test11@test.ru',<br>   position:’developer23’, age:””                       </p><p>3. Вставить объект в  параметр метода Post</p><p>4. Методом Post добавить нового сотрудника</p>|Always|Critical|Missing feature|Нет||
|10|incrt\_4\_4\_2|Создается запросом Post новый сотрудник с возрастом в виде цифровой строчки (“32”)|После отправки Post запроса приходит ответ ошибка с кодом 400|После отправки Post запроса приходит ответ ошибка с кодом 201|<p>1. Создать объект с ключами name:string, email:string, position:string, age:string в формате json</p><p>2. Заполнить объект:<br>   'name':'Max11', email: ‘test10@test.ru',<br>   position:’developer23’, age:”32”                       </p><p>3. Вставить объект в  параметр метода Post</p><p>4. Методом Post добавить нового сотрудника</p>|Always|Critical|Missing feature|Нет||
|11|incrt\_4\_4\_1|Создается запросом Post новый сотрудник с возрастом в виде null|После отправки Post запроса приходит ответ ошибка с кодом 400|После отправки Post запроса приходит ответ ошибка с кодом 201|<p>1. Создать объект с ключами name:string, email:string, position:string, age:null в формате json</p><p>2. Заполнить объект:<br>   'name':'Max10', email: ‘test9@test.ru',<br>   position:’developer23’, age:null                       </p><p>3. Вставить объект в  параметр метода Post</p><p>4. Методом Post добавить нового сотрудника</p>|Always|Critical|Missing feature|Нет||
|12|incrt\_4\_0\_1|Создается новый сотрудник с отсутствующим обязательным ключом name|После отправки Post запроса приходит ответ ошибка с кодом 400|После отправки Post запроса приходит ответ с кодом 201|<p>1. Создать объект с ключами email:string, position:string, age:integer в формате json</p><p>2. Заполнить объект:<br>   email: ‘test14@test.ru',<br>   position:’developer23’, age:30                      </p><p>3. Вставить объект в  параметр метода Post</p><p>4. Методом Post добавить нового сотрудника</p>|Always|Critical|Missing feature|Нет||
|13|incrt\_4\_0\_3|Создается новый сотрудник с отсутствующим обязательным ключом email|После отправки Post запроса приходит ответ ошибка с кодом 400|После отправки Post запроса приходит ответ с кодом 201|<p>1. Создать объект с ключами name:string, position:string, age:integer в формате json</p><p>2. Заполнить объект:<br>   name:’Max16’,<br>   position:’developer23’, age:30                      </p><p>3. Вставить объект в  параметр метода Post</p><p>4. Методом Post добавить нового сотрудника</p>|Always|Critical|Missing feature|Нет||
|14|incrt\_4\_0\_4|Без обязательного ключа age падает крашится сервер|После отправки Post запроса приходит ответ ошибка с кодом 400|После отправки Post запроса приходит ответ с кодом 500|<p>1. Создать объект с ключами name:string, position:string в формате json</p><p>2. Заполнить объект:<br>   name:’Max18’,<br>   'email': '<test16@test.ru>', position:’developer23’,                      </p><p>3. Вставить объект в  параметр метода Post</p><p>4. Методом Post добавить нового сотрудника</p>|Always|Critical|System crash|Нет||
|15|incrt\_4\_0\_6|Создается новый сотрудник с произвольным новым ключом и значением|После отправки Post запроса приходит ответ ошибка с кодом 400|После отправки Post запроса приходит ответ с кодом 201 (создан)|<p>1. Создать объект с ключами name:string, 'email':string, position:string,<br>   isSingle:boolean в формате json</p><p>2. Заполнить объект:<br>   name:’Max20’, email:’'test17@test.ru'’<br>   position:’developer23’, age:30, isSingle:true                      </p><p>3. Вставить объект в  параметр метода Post</p><p>4. Методом Post добавить нового сотрудника</p>|Always|Minor|Missing feature|Нет||
|16|incrt\_4\_0\_7|Создается новый сотрудник с id, заданным вручную|После отправки Post запроса приходит ответ ошибка с кодом 400|После отправки Post запроса приходит ответ с кодом 201 (создан), но id, написанный вручную, заменяется на генерируемый сервером|<p>1. Создать объект с ключами <br>   id:integer<br>   name:string, 'email':string, position:string, age:integer<br>   в формате json</p><p>2. Заполнить объект:<br>   id:111111<br>   name:’Max21’, email:’'test18@test.ru'’<br>   position:’developer23’, age:30,                      </p><p>3. Вставить объект в  параметр метода Post</p><p>4. Методом Post добавить нового сотрудника</p>|Always|Major|Missing feature|Нет||
|17|incrt\_2\_2|Отсутствует проверка на корректность id=null в Get запросе|После отправки Get с id=null приходит ответ ошибка с кодом 404|После отправки Get с id=null приходит ответ ошибка с кодом 500|<p>1. Создать переменную со значением<br>   null                   </p><p>2. Вставить переменную в  параметр метода Get</p><p>3. Методом Get получить нового сотрудника</p>|Always|Critical|Missing feature|Нет|Из за особенностей метода sendGet невозможно отправить id не как строчку. Поскольку он изменяет адресную строку:<br>{url}/employee/?{параметр}={значение}, а у сервера нет таких запросов|
|18|<p>incrt\_2\_6</p><p></p><p></p>|Отсутствует проверка на корректность id=”” в Get запросе|После отправки Get с id=”” приходит ответ ошибка с кодом 404|После отправки Get с id=null приходит ответ ошибка с кодом 500|<p>1. Создать переменную со значением<br>   “”                  </p><p>2. Вставить переменную в  параметр метода Get</p><p>3. Методом Get получить нового сотрудника</p>|Always|Critical|Missing feature|Нет||
|19|incrt\_5\_1, incrt\_5\_3, incrt\_5\_4, |Нет сообщения на отсутствие пользователя для удаления |<p>После отправки Delete с id=</p><p>1. float</p><p>2. -1</p><p>3. true</p><p>приходит ответ ошибка с кодом 404</p>|<p>После отправки Delete с id=</p><p>1. float</p><p>2. -1</p><p>3. true</p><p>приходит ответ ошибка с кодом 204</p><p></p><p></p>|<p>1. Создать переменную со значением<br>   1.12.1<br>   2. -1<br>   3. true                </p><p>2. Вставить переменную в  параметр метода Delete </p><p>3. Методом Delete получить нового сотрудника</p>|Always|Critical|Missing feature|Нет||
|20|incrt\_5\_2 , incrt\_5\_5|Отсутствует проверка на корректность id=”” или id=null в Delete запросе|<p>После отправки Delete с id=</p><p>1. “”</p><p>2. null</p><p></p><p>приходит ответ ошибка с кодом 404</p>|<p>После отправки Delete с id=</p><p>1. “”</p><p>2. null</p><p>приходит ответ ошибка с кодом 500</p><p></p><p></p>|<p>1. Создать переменную со значением<br>   1.””<br>   2. null             </p><p>2. Вставить переменную в  параметр метода Delete </p><p>3. Методом Delete получить нового сотрудника</p>|Always|Critical|Missing feature|Нет||

